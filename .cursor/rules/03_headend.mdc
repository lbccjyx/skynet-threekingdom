---
alwaysApply: true
description: 客户端说明
---

## 客户端目录
client/*


### client/js 下目录结构
core/ (基础配置, 网络, 状态)
game/
managers/ (SceneManager, )
    SceneManager.js：负责管理和切换场景。
    TextureGhostManager.js: 负责2D图片移动位置时 生成一张半透明的2D图片 并且跟随鼠标
scenes/
    BaseScene.js：场景基类，定义了 enter, exit, update, render 等生命周期。
    CityScene.js & MapScene.js：分别处理城内和城外的逻辑与渲染。

entities
    Entity.js：实体基类。
    Building.js：封装了普通建筑的渲染、进度条更新逻辑。
    General.js：封装了武将的渲染逻辑。
    rect_building/        
        RectBuilding.js：封装了圈地（城墙、道路、农田）的渲染逻辑，包括墙体的自动连接算法。
        build_rect.js 实现rect_building的拖拽

render
    render_engine.js 单例，init时创建摄像机等3D场景。成员变量存放所有资源
    render_grids.js 单例，创建2D图片 主要是用于展示场景边界
    render_input.js 单例：获取鼠标位置 处理建筑更新 设置建筑选中状态 更新建筑建筑条 等
    render_rect_building.js 单例：给没有sub_building的rect_building创建3D对象
    render_sub_building.js 单例：给有sub_building的rect_building创建3D对象
    render_texture.js 单例 给2D图片放置在3D环境中 一般图片展示需要用到

input
    d_building_inputs.js: 处理 d_building的拖拽和放置
    input.js: 总的鼠标监听入口  包括 鼠标移动 (悬停, 拖拽, 平移) 会调用到其他函数具体处理

game.js (入口)
logic.js

ui/, / ...

### 资源下载脚本
client/start_server.py 这个脚本执行后是开放8000端口，允许http从client/assets 文件夹下载图片资源等。

### 图片资源
client/assets 这里存放了一些图片资源 如果没有相关图片，可以先用文字或者字母代替

### 客户端样式文件
client/css/style.css

### 客户端网址
http://192.168.3.43:8000/

### 坐标系
右下是X轴正方向
左下是Y轴正方向